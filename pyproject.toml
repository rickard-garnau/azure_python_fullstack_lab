[project]
name = "azure-python-fullstack-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.3.0",
    "pandas>=3.0.5",
    "pathlib>=1.0.1",
]

[tool.uv.workspace]
members = [
    "backend",
    "frontend",
    "infra/backend",
]
